/*
ask the user to enter the radius of a sphere.  
This program will then calculate its volume'

The radius is a floating point number. (it has decimal 
places).

*/

#include <stdio.h>
int main(void){
	double radius;

	printf("Please enter the radius: ");
	scanf("%lf",&radius);
}
